specVersion: "v1alpha"
serviceId: "my-service"
location: "us-west1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fdc-sql"
connectorDirs: ["./default"]
